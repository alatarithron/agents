{
  "name": "agents",
  "private": true,
  "description": "Tooling for the kit's own .sh and .md files. Not a published package.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:md": "markdownlint-cli2"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.9.6",
    "prettier-plugin-sh": "^0.19.0"
  }
}
